# Milestone v1.2: Database Simplification

**Status:** SHIPPED 2026-01-26
**Phases:** 14-15
**Total Plans:** 2

## Overview

Simplify database layer by converting to object format and implementing on-demand file management. This milestone reduces code complexity and filesystem clutter.

## Phases

### Phase 14: Object Format

**Goal**: Convert JSON storage from array format to single object format
**Depends on**: v1.1 complete
**Plans**: 1 plan

Plans:
- [x] 14-01: Object format migration — completed 2026-01-26

**Details:**
- Renamed `ConfigEntry` to `ConfigData` for semantic clarity
- Updated `readCategory`/`writeCategory` to handle object format
- Updated structure scanner, apply service, API routes, presets service
- All 8 files updated for object format consistency

### Phase 15: On-Demand Storage

**Goal**: Create files only when data exists, delete empty files and directories
**Depends on**: Phase 14
**Plans**: 1 plan

Plans:
- [x] 15-01: On-demand storage implementation — completed 2026-01-26

**Details:**
- Added `cleanupEmptyDirectories` helper that walks up directory tree
- Modified `writeCategory` to delete file when data is empty `{}`
- Directory cleanup stops at databases root to preserve it
- All 7 inline tests pass

---

## Milestone Summary

**Key Decisions:**
- Renamed ConfigEntry to ConfigData (semantic clarity)
- cleanupEmptyDirectories stops at getDatabasesRoot() to preserve root
- ENOENT errors ignored during deletion (idempotent behavior)

**Patterns Established:**
- Object format: `{ key: value }` instead of `[{ key: value }]`
- On-demand storage: write empty object deletes file
- Directory cleanup: empty parents removed up to databases root

**Issues Resolved:**
- None (internal refactor, no user-facing issues)

**Issues Deferred:**
- None

**Technical Debt Incurred:**
- None

**Stats:**
- 13 files modified
- +198 / -221 lines (net -23 lines simplification)
- 2 phases, 2 plans, 5 tasks

---

*For current project status, see .planning/ROADMAP.md*
