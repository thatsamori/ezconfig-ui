---
phase: 33-polish
plan: 01
type: execute
---

<objective>
Final UX polish and cleanup for the v2.2 milestone.

Purpose: Address any remaining rough edges from phases 31-32 and ensure consistent user experience
Output: Polished UI with consistent styling and behavior
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Depends on:**
- Phase 31: Rich text notes
- Phase 32: Input debouncing with save indicators

**Polish items to address:**
1. Rich text editor styling consistency with Shadcn
2. Ensure all UI elements match existing design language
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style RichTextEditor to match Shadcn theme</name>
  <files>src/components/notes/RichTextEditor.tsx</files>
  <action>
Ensure RichTextEditor styling is consistent with Shadcn UI:

1. Editor container: Use same border/focus styles as Shadcn Input
   - `border border-input rounded-md focus-within:ring-2 focus-within:ring-ring focus-within:ring-offset-2`
2. Toolbar: Use bg-muted/50 for subtle separation, border-b
3. Toolbar buttons: Match existing icon button styles (h-7 w-7, ghost variant)
4. Editor content area: Appropriate padding (p-3), min-height (min-h-[80px])
5. Placeholder styling: text-muted-foreground
6. Use prose-sm classes for rendered content

Add specific styles for editor content:
```css
/* In component or global CSS */
.ProseMirror {
  outline: none;
}
.ProseMirror p.is-editor-empty:first-child::before {
  content: attr(data-placeholder);
  float: left;
  color: hsl(var(--muted-foreground));
  pointer-events: none;
  height: 0;
}
```
  </action>
  <verify>RichTextEditor looks native to the Shadcn design system</verify>
  <done>Editor styling matches Shadcn theme</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md and ROADMAP.md for v2.2</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Update project documentation to reflect v2.2 completion:

PROJECT.md:
- Update Current State version to v2.2
- Add v2.2 shipped section in details
- Update Active requirements (if any)
- Add validated requirements for notes enhancements and input debouncing

ROADMAP.md:
- Update v2.2 milestone from "in progress" to "SHIPPED"
- Update progress table for phases 31-33

STATE.md:
- Update current position to reflect v2.2 complete
- Update milestone history
- Update progress bar to 100%
  </action>
  <verify>Documentation reflects completed milestone</verify>
  <done>Project documentation updated for v2.2</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete v2.2 Improvements milestone with rich text notes, input debouncing, and polish</what-built>
  <how-to-verify>
    1. Run: bun run dev
    2. Visit: http://localhost:3000
    3. Test Notes:
       - Open any config row notes dialog
       - Type a note with bold/italic/underline formatting
       - Save and verify formatting displays correctly
       - Edit the note, verify formatting preserved
       - Delete a note, verify confirmation dialog appears
       - Cancel delete, verify note still exists
       - Confirm delete, verify note removed
    4. Test Float Inputs:
       - Type in a float input - should feel responsive (no lag)
       - See spinning indicator while typing
       - After 1 second, see green checkmark
       - Checkmark fades after ~1.5 seconds
    5. Test Vector Inputs:
       - Same behavior for VectorInput (3 fields)
       - Same behavior for Vector2DInput (2 fields)
    6. Test FloatArray Inputs:
       - Same behavior for FloatArrayInput
    7. Styling consistency:
       - RichTextEditor matches Shadcn input styling
       - Save indicators don't cause layout shift
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] RichTextEditor styling matches Shadcn theme
- [ ] Manual verification completed (checkpoint approved)
- [ ] PROJECT.md, ROADMAP.md, STATE.md updated
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- v2.2 milestone complete and polished
- Documentation updated
</success_criteria>

<output>
After completion, create `.planning/phases/33-polish/33-01-SUMMARY.md`
</output>
