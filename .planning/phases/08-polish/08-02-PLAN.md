---
phase: 08-polish
plan: 02
type: execute
---

<objective>
Remove deprecated v0.1 compatibility code and improve error handling.

Purpose: Clean up technical debt from the v0.1 → v1.0 transition and improve user feedback.
Output: Leaner codebase without deprecated code, better error messages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-refactor/06-01-SUMMARY.md
@.planning/phases/07-apply-flow/07-01-SUMMARY.md

# Key files:
@src/lib/store/configStore.ts
@src/components/character/CharacterConfigTab.tsx
@src/components/ActionButtons.tsx

**Tech stack available:** Zustand, React 19, Sonner (toasts)
**Established patterns:** Working/saved state model, toast notifications

**Constraining decisions:**
- [Phase 6-01]: zustand/middleware persist for localStorage
- [Phase 6-02]: CharacterConfigTab uses temporary compatibility layer
- [Phase 7-01]: commitWorkingToSaved merges then clears atomically
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated v0.1 store code</name>
  <files>src/lib/store/configStore.ts</files>
  <action>
Remove all deprecated v0.1 compatibility code from configStore.ts:

1. Remove these deprecated state fields:
   - characterValues
   - characterStaged
   - weaponValues
   - weaponStaged
   - selectedWeapons

2. Remove these deprecated methods:
   - setCharacterValue
   - setCharacterStaged
   - setWeaponValue
   - setWeaponStaged
   - setSelectedWeapons
   - initializeFromGameIni
   - getStagedChanges
   - resetToDefaults

3. Remove from initialState object
4. Remove from ConfigState interface
5. Keep all v1.0 code (savedValues, workingValues, etc.)

The deprecated code was marked "will be removed in Phase 7" - it's now Phase 8, safe to remove.
  </action>
  <verify>bun run build succeeds without errors</verify>
  <done>configStore.ts contains only v1.0 state model, no deprecated code</done>
</task>

<task type="auto">
  <name>Task 2: Update CharacterConfigTab to v1.0 API</name>
  <files>src/components/character/CharacterConfigTab.tsx</files>
  <action>
Update CharacterConfigTab to use the v1.0 store API (working/saved values model), matching WeaponAccordion pattern:

1. Remove any references to deprecated store methods
2. Add state for expandedSection (like WeaponAccordion has expandedWeapon)
3. Use useConfigStore selectors for workingValues.character and savedValues.character
4. Add lazy loading for character config categories:
   - Fetch from /api/config/Character/{category} on section expand
   - Use markCategoryLoaded and isCategoryLoaded
5. Use getEffectiveValue pattern (working ?? saved)
6. Call setWorkingValue and removeWorkingValue for onChange/onReset

Pattern reference: WeaponAccordion.tsx

Note: Character has a different structure than weapons - it's not an accordion of multiple characters, it's collapsible sections for Movement/Combat/General. Keep the CollapsibleSection pattern but add lazy loading per section.
  </action>
  <verify>bun run build succeeds, CharacterConfigTab loads and displays config</verify>
  <done>CharacterConfigTab uses v1.0 store API, lazy loads from /api/config/Character/{category}</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cleaned up deprecated code and updated CharacterConfigTab</what-built>
  <how-to-verify>
    1. Run: bun run dev
    2. Visit: http://localhost:3000
    3. Click "Character" tab
    4. Expand Movement section - verify config loads
    5. Expand Combat section - verify config loads
    6. Change a value, verify it's tracked as unsaved
    7. Click Save, verify it saves successfully
    8. Verify no console errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` succeeds without errors
- [ ] configStore.ts has no deprecated v0.1 code
- [ ] CharacterConfigTab works with v1.0 API
- [ ] Save/Reset/Apply buttons work for character config
- [ ] No console errors or warnings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Deprecated code removed
- CharacterConfigTab fully functional with v1.0 API
- Phase 8 complete, milestone v1.0 ready for final testing
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish/08-02-SUMMARY.md`
</output>
