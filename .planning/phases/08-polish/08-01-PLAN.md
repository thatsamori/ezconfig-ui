---
phase: 08-polish
plan: 01
type: execute
---

<objective>
Add search/filter functionality to the weapon accordion for faster weapon discovery.

Purpose: With many weapons, users need a way to quickly find specific weapons by name.
Output: Search input above weapon accordion that filters weapons as user types.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui-refactor/06-02-SUMMARY.md

# Key files:
@src/components/weapons/WeaponAccordion.tsx
@src/components/weapons/WeaponConfigTab.tsx
@src/components/ui/input.tsx

**Tech stack available:** React 19, Shadcn UI (Input component), Tailwind CSS 4
**Established patterns:** Controlled components, accordion with lazy loading

**Constraining decisions:**
- [Phase 6-02]: Accordion with type=single collapsible
- [Phase 6-02]: Lazy load on accordion expand
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search input to WeaponConfigTab</name>
  <files>src/components/weapons/WeaponConfigTab.tsx</files>
  <action>
Add a search input at the top of WeaponConfigTab that filters the weapons list passed to WeaponAccordion:

1. Add state: `const [searchQuery, setSearchQuery] = useState("")`
2. Add Shadcn Input component above WeaponAccordion:
   - Placeholder: "Search weapons..."
   - value={searchQuery}
   - onChange={(e) => setSearchQuery(e.target.value)}
3. Filter weapons before passing to WeaponAccordion:
   - Case-insensitive match on weapon name
   - Filter the GroupedDatabase object keys
   - Pass filtered weapons to WeaponAccordion

Use the existing Input component from @/components/ui/input.
  </action>
  <verify>bun run build succeeds without errors</verify>
  <done>Search input renders, typing filters weapon list</done>
</task>

<task type="auto">
  <name>Task 2: Add empty state when no weapons match</name>
  <files>src/components/weapons/WeaponConfigTab.tsx</files>
  <action>
Handle the case when search filters out all weapons:

1. Check if filteredWeapons has zero keys after filtering
2. Show message: "No weapons match '{searchQuery}'"
3. Style with text-muted-foreground, centered, py-8
4. Only show empty state if there ARE weapons loaded but none match (not during initial load)

Do NOT change WeaponAccordion - handle empty state in WeaponConfigTab before rendering accordion.
  </action>
  <verify>Search for non-existent weapon name shows empty state message</verify>
  <done>Empty state displays when no weapons match search, loading state still works correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Search/filter functionality for weapons</what-built>
  <how-to-verify>
    1. Run: bun run dev
    2. Visit: http://localhost:3000
    3. Verify search input appears above weapon list
    4. Type partial weapon name (e.g., "sword")
    5. Confirm only matching weapons appear
    6. Clear search, all weapons return
    7. Search for nonsense (e.g., "xyzabc")
    8. Confirm empty state message appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` succeeds without errors
- [ ] Search input is visible and functional
- [ ] Weapon filtering is case-insensitive
- [ ] Empty state shows when no weapons match
- [ ] Accordion lazy loading still works after filtering
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Search filters weapons correctly
- Empty state displays appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish/08-01-SUMMARY.md`
</output>
