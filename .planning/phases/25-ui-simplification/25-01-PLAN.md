---
phase: 25-ui-simplification
plan: 01
type: execute
---

<objective>
Remove Save/Reset buttons and update UI components to use new store method names.

Purpose: Phase 24 removed the working/saved state split. UI still uses deprecated aliases. This plan updates all UI components to use the new method names and removes the now-unnecessary Save/Reset buttons.
Output: Simplified ActionButtons, updated config tabs and preset components using new store API.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-state-model-removal/24-01-SUMMARY.md
@.planning/phases/24-state-model-removal/24-02-SUMMARY.md

**Key context from Phase 24:**
- configStore now has single `values` state (no working/saved split)
- `setValue` writes optimistically to local state + async API write
- `removeValue` same pattern
- `hasUnsavedChanges` always returns false
- Deprecated aliases exist: workingValues, savedValues, setWorkingValue, setSavedValue, getEffectiveValue, resetWorkingValues, commitWorkingToSaved, removeWorkingValue, clearSavedCategory

**Files to modify:**
@src/components/ActionButtons.tsx
@src/components/character/CharacterConfigTab.tsx
@src/components/weapons/WeaponAccordion.tsx
@src/components/presets/PresetsTab.tsx
@src/components/presets/SavePresetDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify ActionButtons - remove Save/Reset buttons</name>
  <files>src/components/ActionButtons.tsx</files>
  <action>
Remove the Save Changes and Reset Working Changes buttons since changes now auto-save to database.

1. Remove all deprecated store method imports/usage:
   - Remove: hasUnsavedChanges, workingValues, savedValues, resetWorkingValues, commitWorkingToSaved
   - Remove: buildMergedEntries helper function
   - Remove: handleSave function
   - Remove: handleReset function
   - Remove: isSaving state

2. Remove the Save button and its disabled logic

3. Remove the Reset button and its AlertDialog

4. Update Apply button:
   - Remove `disabled={hasUnsavedChanges}` - button should always be clickable now
   - Keep the SelectiveApplyDialog integration unchanged

5. Clean up unused imports (AlertDialog components, useState for isSaving)

Result: ActionButtons should only contain the Apply to Game button + SelectiveApplyDialog.
  </action>
  <verify>npm run build passes, app loads, Apply button visible and clickable</verify>
  <done>ActionButtons contains only Apply button, no Save/Reset, no deprecated store usage</done>
</task>

<task type="auto">
  <name>Task 2: Update CharacterConfigTab to use new store methods</name>
  <files>src/components/character/CharacterConfigTab.tsx</files>
  <action>
Replace deprecated aliases with new method names:

1. In useCharacterConfigLoader hook:
   - Change `setSavedValue` to `setValue` (both from useConfigStore destructuring)

2. In CharacterCategoryPanel component:
   - Change `workingValues` → `values` (useConfigStore selector)
   - Change `savedValues` → `values` (useConfigStore selector - can use same variable now)
   - Change `setWorkingValue` → `setValue`
   - Change `removeWorkingValue` → `removeValue`

3. Update getEffectiveValue helper:
   - Remove the tombstone (null) check logic since there's only one state now
   - Simplify to just return `values.character[category]?.[key]`
   - Or better: use store's `getValue` method directly instead of local helper

4. Update all usages of these methods in the component.
  </action>
  <verify>npm run build passes, Character tab loads, can edit character config values</verify>
  <done>CharacterConfigTab uses only: values, setValue, removeValue, getValue - no deprecated aliases</done>
</task>

<task type="auto">
  <name>Task 3: Update WeaponAccordion to use new store methods</name>
  <files>src/components/weapons/WeaponAccordion.tsx</files>
  <action>
Replace deprecated aliases with new method names (same pattern as CharacterConfigTab):

1. In useWeaponConfigLoader hook:
   - Change `setSavedValue` → `setValue`

2. In WeaponConfigPanel component:
   - Change `workingValues` → `values`
   - Change `savedValues` → `values` (same variable)
   - Change `setWorkingValue` → `setValue`
   - Change `removeWorkingValue` → `removeValue`

3. Update getEffectiveValue helper:
   - Simplify to return `values.weapons[weaponName]?.[category]?.[key]`
   - Or use store's `getValue` method directly

4. Update all usages in the component.
  </action>
  <verify>npm run build passes, Weapons tab loads, can expand accordion, can edit weapon config values</verify>
  <done>WeaponAccordion uses only: values, setValue, removeValue, getValue - no deprecated aliases</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] App loads without console errors
- [ ] Character config tab: can load, edit, and see values persist
- [ ] Weapons config tab: can expand accordion, edit values, see persistence
- [ ] Apply to Game button is always enabled and opens dialog
- [ ] No references to deprecated methods in modified files
</verification>

<success_criteria>
- All 3 tasks completed
- ActionButtons shows only Apply button
- Character and Weapon tabs use new store method names
- All verification checks pass
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-ui-simplification/25-01-SUMMARY.md`
</output>
