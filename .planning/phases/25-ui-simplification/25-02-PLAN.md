---
phase: 25-ui-simplification
plan: 02
type: execute
---

<objective>
Update preset components and clean up configStore deprecated aliases.

Purpose: Complete the UI migration by updating preset components, then remove the deprecated aliases from configStore that are no longer needed.
Output: Clean store interface with only the new methods, all UI components using new API.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-ui-simplification/25-01-PLAN.md

**Depends on:** 25-01-PLAN.md completion (UI components updated)

**Files to modify:**
@src/components/presets/PresetsTab.tsx
@src/components/presets/SavePresetDialog.tsx
@src/lib/store/configStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PresetsTab to use new store methods</name>
  <files>src/components/presets/PresetsTab.tsx</files>
  <action>
Replace deprecated aliases:

1. Remove `hasUnsavedChanges` usage:
   - Line 57: Remove `hasUnsavedChanges` from useConfigStore
   - Line 98: Remove the `if (hasUnsavedChanges)` check before loading preset
   - The preset can be loaded at any time now since changes auto-save

2. Change `savedValues` → `values`:
   - Line 58: Change selector from `state.savedValues` to `state.values`
   - Update variable name from `savedValues` to `values`
   - Lines 64-65: Update references in `hasSavedConfig` check

3. Remove `resetWorkingValues` usage:
   - Line 59: Remove from useConfigStore
   - Line 177: Remove the call to `resetWorkingValues()` in handleLoadPreset
   - The loadPreset function already replaces all values, no reset needed

4. Update any other references to use new names.
  </action>
  <verify>npm run build passes, Presets tab loads, can load/save presets</verify>
  <done>PresetsTab uses only: values, loadPreset - no deprecated aliases</done>
</task>

<task type="auto">
  <name>Task 2: Update SavePresetDialog to use new store methods</name>
  <files>src/components/presets/SavePresetDialog.tsx</files>
  <action>
Simple rename:

1. Line 33: Change `savedValues` → `values` in useConfigStore selector
   - From: `const savedValues = useConfigStore((state) => state.savedValues);`
   - To: `const values = useConfigStore((state) => state.values);`

2. Line 85: Update reference in save handler
   - From: `data: savedValues,`
   - To: `data: values,`
  </action>
  <verify>npm run build passes, can save current config as preset</verify>
  <done>SavePresetDialog uses `values` instead of `savedValues`</done>
</task>

<task type="auto">
  <name>Task 3: Remove deprecated aliases from configStore</name>
  <files>src/lib/store/configStore.ts</files>
  <action>
Now that all UI components use the new methods, remove deprecated aliases:

1. From ConfigState interface (lines 29-39), remove:
   - hasUnsavedChanges: boolean
   - workingValues: ConfigState['values']
   - savedValues: ConfigState['values']
   - setWorkingValue: (...)
   - setSavedValue: (...)
   - getEffectiveValue: (...)
   - resetWorkingValues: ()
   - commitWorkingToSaved: ()
   - removeWorkingValue: (...)
   - clearSavedCategory: (...)

2. From initialState (lines 68-75), remove:
   - workingValues: initialValues
   - savedValues: initialValues
   - hasUnsavedChanges: false

3. In all set() calls, remove the deprecated alias updates:
   - Lines like `workingValues: newValues, savedValues: newValues`
   - Just keep `values: newValues`

4. Remove the deprecated method implementations (lines 375-410):
   - setWorkingValue
   - setSavedValue
   - getEffectiveValue
   - resetWorkingValues
   - commitWorkingToSaved
   - removeWorkingValue
   - clearSavedCategory

5. Remove comments referencing Phase 25 cleanup.

Result: Clean ConfigState interface with only the essential methods.
  </action>
  <verify>npm run build passes, app fully functional (all tabs work, presets work, apply works)</verify>
  <done>configStore has no deprecated aliases, clean interface with: values, setValue, removeValue, getValue, loadPreset, etc.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] App loads without console errors
- [ ] Character config: load, edit, values persist
- [ ] Weapons config: expand accordion, edit, values persist
- [ ] Presets: load preset replaces values, save preset captures current values
- [ ] Apply to Game: dialog opens, commands listed, apply works
- [ ] No "deprecated" comments remain in configStore
</verification>

<success_criteria>
- All 3 tasks completed
- configStore interface is clean (no deprecated methods)
- All preset functionality works
- All verification checks pass
- Phase 25 complete
</success_criteria>

<output>
After completion, create `.planning/phases/25-ui-simplification/25-02-SUMMARY.md`
</output>
