---
phase: 13-preset-polish
plan: 01-FIX
type: fix
source: 13-01-ISSUES.md
---

<objective>
Fix UAT-001: Block preset loading when unsaved changes exist, prompting user to save or reset first.

Purpose: Prevent accidental data loss by ensuring users consciously handle unsaved changes before loading a preset.
Output: Updated PresetsTab that blocks preset loading until changes are resolved.
</objective>

<context>
@.planning/phases/13-preset-polish/13-01-ISSUES.md
@src/components/presets/PresetsTab.tsx
@src/lib/store/configStore.ts
</context>

<issues>
- UAT-001 (Major): Preset load with unsaved changes should block, not offer discard option
</issues>

<tasks>

<task type="auto">
  <name>Task 1: Update unsaved changes dialog to block loading</name>
  <files>src/components/presets/PresetsTab.tsx</files>
  <action>
Modify the unsaved changes AlertDialog to block preset loading instead of offering "Discard & Load":

1. Update AlertDialogDescription to explain the blocking behavior:
   - "You have unsaved changes that must be resolved before loading a preset."

2. Replace "Discard & Load" button with two options:
   - "Save Changes" - Navigates user to save their current config (or shows save flow)
   - "Reset Changes" - Calls resetWorkingValues() to discard working changes

3. After either action completes:
   - Close the unsaved dialog
   - Do NOT automatically open preview
   - User must click Load again (now with clean state)

4. Remove handleDiscardAndLoad function since we no longer offer that flow.

5. Update handleCloseDialog to properly reset state.

The key behavior change:
- OLD: Warning → Discard & Load → loads preset immediately
- NEW: Warning → (Save or Reset) → dialog closes → user clicks Load again → preview shows
  </action>
  <verify>npm run build succeeds, clicking Load with unsaved changes shows blocking dialog without "Discard & Load"</verify>
  <done>Unsaved changes dialog blocks loading, offers Save or Reset options only</done>
</task>

</tasks>

<verification>
Before declaring fix complete:
- [ ] `npm run build` succeeds without errors
- [ ] With unsaved changes, clicking Load shows blocking dialog
- [ ] No "Discard & Load" option exists
- [ ] "Reset Changes" discards working values and closes dialog
- [ ] After reset, clicking Load again shows preview dialog
- [ ] Flow for no unsaved changes unchanged (Load → Preview)
</verification>

<success_criteria>
- Unsaved changes completely block preset loading
- User must explicitly save or reset before they can load
- Preview dialog still works when no unsaved changes exist
- No accidental data loss possible
</success_criteria>

<output>
After fix verified, move UAT-001 to "Resolved Issues" section in 13-01-ISSUES.md
</output>
