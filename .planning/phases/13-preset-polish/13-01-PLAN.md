---
phase: 13-preset-polish
plan: 01
type: execute
---

<objective>
Add preset preview before loading, so users can see what a preset contains before committing to load it.

Purpose: Users should understand what changes a preset will make before replacing their current configuration.
Output: PresetPreviewDialog component integrated into the load flow.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries (preset system):
@.planning/phases/11-static-presets/11-02-SUMMARY.md
@.planning/phases/12-user-presets/12-02-SUMMARY.md

# Current implementation:
@src/components/presets/PresetsTab.tsx
@src/lib/presets/types.ts
@src/lib/presets/service.ts

**Tech stack available:** React 19, Shadcn UI, Zustand, Tailwind CSS 4
**Established patterns:** Two-stage dialog flow, conditional AlertDialog based on store state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PresetPreviewDialog component</name>
  <files>src/components/presets/PresetPreviewDialog.tsx, src/components/presets/index.ts</files>
  <action>
Create PresetPreviewDialog component using Shadcn Dialog:

1. Props interface:
   - `open: boolean`
   - `onOpenChange: (open: boolean) => void`
   - `preset: PresetInfo | null`
   - `presetType: "static" | "user" | null`
   - `onConfirmLoad: () => void`
   - `isLoading: boolean`

2. Content structure:
   - Dialog header with preset title and description from manifest
   - Content section showing preview summary:
     - "Character Settings" heading with count of categories and total settings
     - "Weapon Settings" heading with list of weapons and their category counts
   - Use a simple list or grid layout, nothing fancy

3. Fetch preset data on open:
   - When dialog opens and preset is set, fetch from appropriate endpoint
   - Show loading spinner while fetching
   - Parse PresetData to generate summary counts

4. Summary calculation helper:
   - Count character categories and total keys per category
   - Count weapons and categories per weapon
   - Display as: "Movement (5 settings), Combat (3 settings)" etc.

5. Footer with Cancel and Load buttons:
   - Cancel closes dialog
   - Load calls onConfirmLoad (parent handles actual loading)

Use existing Dialog component from shadcn. Keep styling consistent with other dialogs in the app.
  </action>
  <verify>TypeScript compiles without errors, component exports from index.ts</verify>
  <done>PresetPreviewDialog component created with manifest display and content summary</done>
</task>

<task type="auto">
  <name>Task 2: Integrate preview into PresetsTab load flow</name>
  <files>src/components/presets/PresetsTab.tsx</files>
  <action>
Replace direct load confirmation with preview dialog:

1. Add state for preview dialog:
   - `previewOpen: boolean`
   - Remove or repurpose `dialogType === "confirm"` flow

2. Update handleLoadClick:
   - If hasUnsavedChanges, still show unsaved warning first
   - After unsaved warning (or if no unsaved changes), open preview dialog instead of confirm dialog
   - Pass selectedPreset and presetType to PresetPreviewDialog

3. Update handleDiscardAndLoad:
   - After resetting working values, open preview dialog instead of confirm dialog

4. Wire up PresetPreviewDialog:
   - open={previewOpen}
   - onOpenChange for closing
   - onConfirmLoad calls handleConfirmLoad (existing function)
   - Pass isLoadingPreset for loading state

5. Remove the old "confirm" AlertDialog since preview dialog replaces it.

Flow should be:
- Click Load → (if unsaved: show warning → Discard & Load) → Preview Dialog → Load
- Click Load → (if no unsaved) → Preview Dialog → Load
  </action>
  <verify>npm run build succeeds, clicking Load shows preview dialog with preset contents</verify>
  <done>Load flow shows preview before loading, unsaved warning still works, old confirm dialog removed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors
- [ ] Clicking Load on any preset shows PresetPreviewDialog
- [ ] Preview shows preset title, description, and content summary
- [ ] Unsaved changes warning still appears before preview when applicable
- [ ] Confirm load from preview actually loads the preset
</verification>

<success_criteria>
- PresetPreviewDialog component created and exported
- Load flow updated to show preview before loading
- Content summary shows character categories and weapon counts
- Existing unsaved changes protection preserved
- No regressions in preset load functionality
</success_criteria>

<output>
After completion, create `.planning/phases/13-preset-polish/13-01-SUMMARY.md`
</output>
