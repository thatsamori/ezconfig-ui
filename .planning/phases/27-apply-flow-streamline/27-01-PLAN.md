---
phase: 27-apply-flow-streamline
plan: 01
type: execute
---

<objective>
Update apply flow terminology and comments to reflect v2.0 model where all changes persist immediately to database.

Purpose: Remove outdated "working vs saved" language from apply-related code after v2.0 state model simplification.
Output: Clean terminology throughout apply flow components and services.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-preset-flow-update/26-01-SUMMARY.md

**Context from prior phases:**
- Phase 24: Removed working state model, all edits write directly to database
- Phase 25: Removed Save/Reset buttons from UI
- Phase 26: Updated preset components to use new store API

**What changed in v2.0:**
- No more "working vs saved" distinction - database IS the current state
- No more "unsaved changes" concept - changes persist immediately
- Apply dialog just sends current database config to game

**Files to update:**
@src/lib/database/apply.ts - Comments reference "saved config"
@src/components/SelectiveApplyDialog.tsx - Dialog description mentions "changes"
@src/app/api/apply/route.ts - Comment mentions "saved config"
@src/app/api/apply/preview/route.ts - Comment mentions "saved config" context
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update apply service terminology</name>
  <files>src/lib/database/apply.ts</files>
  <action>Update comments to remove "saved" terminology:
- Line 4 docstring: "Send saved config to game" → "Send config to game"
- Line 151 docstring: "Apply all saved config" → "Apply all config"
These are comments only - no functional changes.</action>
  <verify>Read file and verify comments updated, `npm run build` passes</verify>
  <done>Comments no longer reference "saved" state, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Update API route comments</name>
  <files>src/app/api/apply/route.ts, src/app/api/apply/preview/route.ts</files>
  <action>Update comments:
- route.ts line 10: "build from saved config" → "build from config"
- route.ts line 50-53: comments about "saved config" → just "config"
- preview/route.ts line 6: reference to SelectiveApplyDialog context is fine, but ensure no "saved" terminology</action>
  <verify>`npm run build` passes</verify>
  <done>API route comments updated, no "saved" terminology</done>
</task>

<task type="auto">
  <name>Task 3: Update SelectiveApplyDialog text</name>
  <files>src/components/SelectiveApplyDialog.tsx</files>
  <action>Update user-facing text:
- DialogDescription (line 177): "Select which configuration changes to send" → "Select which configuration to send" (not "changes" since everything is current config)
- Review other user-facing strings for "saved" or "changes" terminology that no longer applies</action>
  <verify>`npm run build` passes, review dialog text is accurate</verify>
  <done>Dialog text reflects that this is current config, not "changes"</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] No references to "saved config" or "saved values" in apply-related files
- [ ] User-facing text in SelectiveApplyDialog is accurate for v2.0 model
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Apply flow terminology consistent with v2.0 immediate-persistence model
</success_criteria>

<output>
After completion, create `.planning/phases/27-apply-flow-streamline/27-01-SUMMARY.md`
</output>
