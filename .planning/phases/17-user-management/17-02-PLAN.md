---
phase: 17-user-management
plan: 02
type: execute
---

<objective>
Create Users tab UI for global_admin to manage users.

Purpose: Enable admins to view, create, edit, and delete users through the web interface.
Output: Users tab with user table, create/edit dialogs, and delete confirmation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-user-management/17-01-PLAN.md

# UI patterns to follow:
@src/components/presets/PresetsTab.tsx - Tab structure, dialogs, CRUD patterns
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/components/ui/input.tsx

# Auth store for role checking:
@src/lib/store/authStore.ts

**From 17-01:**
- GET /api/users - list users
- POST /api/users - create user
- PUT /api/users/[username] - update user
- DELETE /api/users/[username] - delete user

**Roles:**
- viewer, preset_creator, config_editor, global_admin
- Only global_admin can access Users tab
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Users tab components</name>
  <files>src/components/users/UsersTab.tsx, src/components/users/UserDialog.tsx, src/components/users/index.ts</files>
  <action>
1. Create src/components/users/UsersTab.tsx:
   - Fetch users from GET /api/users on mount
   - Display users in a table: Username, Role, Actions (Edit, Delete)
   - "Create User" button opens UserDialog
   - Edit button opens UserDialog with user data
   - Delete button shows confirmation dialog
   - Use existing UI components (Card, Button, AlertDialog)
   - Handle loading and error states
   - Refresh list after create/edit/delete

2. Create src/components/users/UserDialog.tsx:
   - Dialog for create/edit user
   - Props: { open, onOpenChange, user?: User, onSaved: () => void }
   - If user prop: edit mode (username readonly, password optional)
   - If no user prop: create mode (all fields required)
   - Fields: username, password, role (select dropdown)
   - Submit calls POST or PUT as appropriate
   - Show error toast on failure, success toast on save

3. Create src/components/users/index.ts barrel export

Use patterns from PresetsTab.tsx (fetch, dialogs, CRUD flow).
Use sonner for toasts.
  </action>
  <verify>Components exist, no TypeScript errors</verify>
  <done>UsersTab and UserDialog components created with full CRUD functionality</done>
</task>

<task type="auto">
  <name>Task 2: Add Users tab to main page (admin only)</name>
  <files>src/app/page.tsx</files>
  <action>
1. Import UsersTab component

2. Add Users tab to TabsList (after Presets):
   - Only show tab if user.role === 'global_admin'
   - Tab label: "Users"

3. Add TabsContent for users tab

The tab should only be visible to global_admin users.
Other roles won't see the tab at all.
  </action>
  <verify>npm run dev, login as admin, Users tab visible and functional</verify>
  <done>Users tab appears for global_admin, hidden for other roles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete user management UI with CRUD operations</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Login as admin (set ADMIN_USERNAME/ADMIN_PASSWORD env vars)
    3. Verify: "Users" tab appears in navigation
    4. Click Users tab → see user table with admin user listed
    5. Click "Create User" → fill form with test/test123/viewer → submit
    6. Verify: New user appears in table
    7. Click Edit on test user → change role to config_editor → save
    8. Verify: Role updated in table
    9. Click Delete on test user → confirm → user removed
    10. Logout → login as a non-admin user → verify Users tab is NOT visible
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Users tab visible only for global_admin
- [ ] User list displays correctly
- [ ] Create user works
- [ ] Edit user works
- [ ] Delete user works (with confirmation)
- [ ] Cannot delete yourself
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Phase 17 complete - full user management
</success_criteria>

<output>
After completion, create `.planning/phases/17-user-management/17-02-SUMMARY.md`

Include in summary:
- Users tab with full CRUD UI
- Admin-only visibility
- Ready for Phase 18 role enforcement
</output>
