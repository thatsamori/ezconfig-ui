---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Initialize Next.js project with Tailwind, Shadcn UI, and Zustand.

Purpose: Establish the core framework and tooling that all UI components will build upon.
Output: Working Next.js app with Shadcn components initialized and Zustand installed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Existing files to preserve:**
- `types.ts` - Core type definitions (ConfigEntry, DataType enum)
- `characterConfigSchema.ts` - Character config schema (532 lines)
- `weaponConfigSchema.ts` - Weapon config schema (497 lines)
- `rconExamples.ts` - RCON client integration (gitignored)

**Tech stack (non-negotiable):**
- Bun runtime
- Next.js (App Router)
- TypeScript
- Tailwind CSS
- Shadcn UI
- Zustand

**Key constraints:**
- Must use Bun, not npm/yarn
- Existing TypeScript files must be moved into Next.js src/ structure
- rcon-client package already installed, keep it
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js with Bun</name>
  <files>package.json, next.config.ts, tsconfig.json, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
Run `bunx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-bun --yes` to initialize Next.js in current directory.

This will:
- Set up Next.js with App Router
- Configure Tailwind CSS
- Enable TypeScript with strict mode
- Use src/ directory structure
- Configure @ import alias

After initialization:
- Move existing schema files to `src/lib/config/`: types.ts, characterConfigSchema.ts, weaponConfigSchema.ts
- Keep rconExamples.ts at root (gitignored, will refactor later)
- Ensure rcon-client remains in dependencies

Note: create-next-app will update package.json - verify rcon-client dependency persists.
  </action>
  <verify>
- `bun run dev` starts without errors
- Visit http://localhost:3000 shows Next.js default page
- `ls src/lib/config/` shows types.ts, characterConfigSchema.ts, weaponConfigSchema.ts
  </verify>
  <done>Next.js app running, existing schemas preserved in src/lib/config/</done>
</task>

<task type="auto">
  <name>Task 2: Add Shadcn UI and Zustand</name>
  <files>components.json, src/lib/utils.ts, tailwind.config.ts, src/app/globals.css</files>
  <action>
1. Initialize Shadcn UI:
   `bunx --bun shadcn@latest init`
   - Style: Default
   - Base color: Slate
   - CSS variables: Yes
   - Prefix: (none)
   - src/components path
   - Import alias: @/components

2. Install Zustand:
   `bun add zustand`

3. Add initial Shadcn components that will be needed:
   `bunx --bun shadcn@latest add button input tabs switch card collapsible checkbox`

This sets up the component library and state management for subsequent phases.
  </action>
  <verify>
- `components.json` exists with correct paths
- `src/components/ui/button.tsx` exists
- `bun run build` succeeds without errors
- Import test: add `import { create } from 'zustand'` to any file, no type errors
  </verify>
  <done>Shadcn initialized with base components, Zustand installed, build passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run dev` starts dev server successfully
- [ ] `bun run build` completes without errors
- [ ] Visit http://localhost:3000 loads without errors
- [ ] Shadcn components available in src/components/ui/
- [ ] Existing schema files accessible at src/lib/config/
- [ ] TypeScript compilation has no errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript or build errors
- Project structure ready for UI development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
