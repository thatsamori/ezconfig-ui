---
phase: 12-user-presets
plan: 02
type: execute
---

<objective>
Create UI for saving current config as preset and managing user presets (delete).

Purpose: Enable users to save their configuration and manage their saved presets.
Output: "Save as Preset" button and user preset management in PresetsTab.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-user-presets/12-01-SUMMARY.md

# Key files:
@src/components/presets/PresetsTab.tsx
@src/components/ActionButtons.tsx
@src/lib/store/configStore.ts
@src/app/api/presets/user/route.ts

**Tech stack available:** Next.js 16, React 19, Zustand, Shadcn UI (Dialog, AlertDialog, Input, Button)
**Established patterns:**
- Two-stage dialog flow for safe operations
- Toast feedback for actions
- Card gallery for presets
**Key insight:** Save creates preset from current savedValues (not workingValues - saved state is the "real" config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Save as Preset" button and dialog</name>
  <files>src/components/presets/SavePresetDialog.tsx, src/components/presets/index.ts</files>
  <action>
Create a SavePresetDialog component that allows users to save their current config as a preset:

1. Create SavePresetDialog.tsx:
   - Props: `open: boolean, onOpenChange: (open: boolean) => void`
   - State: name, title, description, isSaving
   - Use Shadcn Dialog with form fields:
     - Name (required): text input with validation hint "alphanumeric, hyphens, underscores"
     - Title (required): text input for display name
     - Description (optional): textarea
   - On save:
     - Get savedValues from configStore
     - POST to /api/presets/user with { name, title, description, data: savedValues }
     - Show toast success/error
     - Close dialog on success
     - Call optional onSaved callback to refresh preset list

2. Validation:
   - Name must match /^[a-zA-Z0-9_-]+$/ (show error if invalid)
   - Title required (show error if empty)
   - Disable Save button while invalid or saving

3. Update index.ts to export SavePresetDialog
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>
    - SavePresetDialog component created
    - Validation prevents invalid preset names
    - Save posts to API and shows feedback
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PresetsTab with user presets section and save button</name>
  <files>src/components/presets/PresetsTab.tsx</files>
  <action>
Update PresetsTab to show user presets separately and add save functionality:

1. Add state for SavePresetDialog (open/closed)
2. Update fetch to get both static and user presets from new API response structure
3. Layout changes:
   - Add "Save Current Config as Preset" button at top (opens SavePresetDialog)
   - Section 1: "Your Presets" - user presets with Load and Delete buttons
   - Section 2: "Built-in Presets" - static presets with Load button only
   - Show "No saved presets yet" if user presets empty

4. User preset cards:
   - Same card layout as static presets
   - Add delete button (trash icon or "Delete" text)
   - On delete: confirm with AlertDialog, then DELETE /api/presets/user/[name]
   - Refresh preset list after delete

5. When SavePresetDialog saves successfully, refresh the preset list

6. Disable "Save Current Config as Preset" if savedValues is empty (nothing to save)
  </action>
  <verify>npm run build succeeds</verify>
  <done>
    - Save as Preset button opens dialog
    - User presets shown in separate section
    - Delete works for user presets
    - Static presets unchanged (no delete)
    - List refreshes after save/delete
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] No TypeScript errors
- [ ] Save dialog validates input
- [ ] User presets appear in their own section
- [ ] Delete removes preset and refreshes list
- [ ] Phase 12 complete
</verification>

<success_criteria>

- Save as Preset functionality works end-to-end
- User presets displayed separately from static presets
- Delete functionality for user presets
- Clear UI feedback (toasts, dialogs)
- Phase 12 complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-user-presets/12-02-SUMMARY.md`
</output>
