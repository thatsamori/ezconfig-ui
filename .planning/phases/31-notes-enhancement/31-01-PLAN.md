---
phase: 31-notes-enhancement
plan: 01
type: execute
---

<objective>
Enhance the notes system with rich text editing and delete confirmation.

Purpose: Improve notes UX with formatted text (bold, italic, underline) and protect against accidental deletion
Output: Updated NotesDialog with Tiptap editor and delete confirmation dialog
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-notes-ui/29-01-SUMMARY.md

**Key files:**
@src/components/notes/NotesDialog.tsx
@src/components/notes/NotesButton.tsx
@src/lib/notes/types.ts

**Tech stack available:** Next.js 16, React 19, Tailwind CSS 4, Shadcn UI
**New dependency:** @tiptap/react, @tiptap/pm, @tiptap/starter-kit, @tiptap/extension-underline

**Constraining decisions:**
- Notes stored as plain text currently (will change to HTML)
- Fire-and-forget API writes pattern
- User ownership check for edit/delete

**Pattern from Context7:**
```tsx
import { useEditor, EditorContent } from '@tiptap/react'
import StarterKit from '@tiptap/starter-kit'

const editor = useEditor({
  extensions: [StarterKit, Underline],
  content: '<p>Hello World!</p>',
})
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tiptap dependencies</name>
  <files>package.json</files>
  <action>Run `bun add @tiptap/react @tiptap/pm @tiptap/starter-kit @tiptap/extension-underline` to install Tiptap and the underline extension.</action>
  <verify>bun run build completes without errors</verify>
  <done>Tiptap packages in dependencies, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create RichTextEditor component with toolbar</name>
  <files>src/components/notes/RichTextEditor.tsx</files>
  <action>
Create a RichTextEditor component that wraps Tiptap with a simple toolbar.

Props:
- `content: string` - initial HTML content
- `onChange: (html: string) => void` - callback when content changes
- `placeholder?: string` - placeholder text

Implementation:
1. Use `useEditor` hook with StarterKit + Underline extension
2. Add simple toolbar with Bold (B), Italic (I), Underline (U) buttons using Shadcn Button
3. Use `editor.chain().focus().toggleBold().run()` pattern for toggles
4. Style active buttons with `editor.isActive('bold')` check
5. Use `onUpdate` callback to call `onChange` with `editor.getHTML()`
6. Render EditorContent with appropriate styling (border, padding, min-height)
7. Add `immediatelyRender: false` option to avoid SSR hydration issues

Style the toolbar buttons to be small (h-6 w-6) and use ghost variant. Use lucide-react icons (Bold, Italic, Underline).
  </action>
  <verify>Component exports correctly, TypeScript compiles without errors</verify>
  <done>RichTextEditor component created with working toolbar</done>
</task>

<task type="auto">
  <name>Task 3: Update NotesDialog to use RichTextEditor and add delete confirmation</name>
  <files>src/components/notes/NotesDialog.tsx</files>
  <action>
Modify NotesDialog to:

1. Replace Textarea with RichTextEditor for note input
2. Store HTML content instead of plain text (noteText state)
3. Update handleSubmit to trim HTML content properly
4. Update note display to render HTML safely using `dangerouslySetInnerHTML`
5. Add delete confirmation using Shadcn AlertDialog:
   - When delete button clicked, show AlertDialog: "Delete this note? This action cannot be undone."
   - Confirm button triggers actual deletion
   - Cancel dismisses dialog
6. Style the rendered HTML notes appropriately (prose classes or custom styles)

For HTML rendering: Create a simple wrapper div with prose-sm classes to format the rich text nicely.

For empty check: Use a helper to detect if HTML content is empty (only contains `<p></p>` or whitespace).
  </action>
  <verify>Notes can be created with formatting, edit existing notes, delete with confirmation</verify>
  <done>NotesDialog uses RichTextEditor, delete requires confirmation</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `bun run build` succeeds without errors
- [ ] Can create a note with bold/italic/underline formatting
- [ ] Formatted notes display correctly in the list
- [ ] Can edit existing notes (formatting preserved)
- [ ] Delete button shows confirmation dialog
- [ ] Canceling delete dismisses dialog without deleting
- [ ] Confirming delete removes the note
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Rich text notes work end-to-end
- Delete confirmation protects against accidental deletion
</success_criteria>

<output>
After completion, create `.planning/phases/31-notes-enhancement/31-01-SUMMARY.md`
</output>
