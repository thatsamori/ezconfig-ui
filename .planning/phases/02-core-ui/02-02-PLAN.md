---
phase: 02-core-ui
plan: 02
type: execute
---

<objective>
Build the weapon configuration tab with multi-weapon selection and collapsible config sections.

Purpose: Enable users to select multiple weapons and edit their configuration values with staged checkboxes.
Output: Complete WeaponConfigTab component with weapon selector, collapsible sections for each attack type, and per-option staged checkboxes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./02-02-SUMMARY.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-ui/02-01-SUMMARY.md

@src/lib/config/weaponConfigSchema.ts
@src/lib/config/types.ts
@src/lib/store/configStore.ts
@src/components/config/index.ts
@src/components/ui/checkbox.tsx
@src/components/ui/collapsible.tsx
@src/components/ui/card.tsx

**Tech stack available:** Next.js 16, React 19, Tailwind CSS 4, Shadcn UI, Zustand 5
**Established patterns:** Config components in src/components/config/, store actions for staged state
**Schema groups:** General, Strike, AltStrike, Stab, AltStab (from WeaponConfigGroupName enum)
**Multi-select behavior:** Changes to any config apply to ALL selected weapons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WeaponSelector multi-select component</name>
  <files>src/components/weapons/WeaponSelector.tsx, src/components/weapons/index.ts</files>
  <action>
Create WeaponSelector that displays all weapons from WeaponName enum as checkboxes.
Uses selectedWeapons from store and setSelectedWeapons action.
Layout: horizontal flex wrap of labeled checkboxes.
Import WeaponName enum from @/lib/config/weaponConfigSchema.
Create barrel export index.ts.
Note: Currently only GreatSword exists in enum - component should handle any number of weapons.
  </action>
  <verify>TypeScript compiles: bun run build</verify>
  <done>WeaponSelector shows checkboxes for each weapon, selection updates store</done>
</task>

<task type="auto">
  <name>Task 2: Create ConfigRow and CollapsibleSection components</name>
  <files>src/components/weapons/ConfigRow.tsx, src/components/weapons/CollapsibleSection.tsx, src/components/weapons/index.ts</files>
  <action>
Create ConfigRow - renders single config option:
- Staged checkbox (left) - controls whether this option is included in apply
- Label (config key name)
- Type-appropriate input component (right) - based on dataType from schema
- Props: configEntry (from schema), value, staged, onValueChange, onStagedChange, disabled

Create CollapsibleSection wrapping Shadcn Collapsible:
- Header shows section name with chevron icon
- Content shows list of ConfigRow components
- Props: title, children, defaultOpen (default true for General, false for attacks)

ConfigRow should map DataType enum to appropriate input component:
- Boolean → BooleanInput
- Float → FloatInput
- Vector → VectorInput
- Vector2D → Vector2DInput
- FloatArray → FloatArrayInput

Update barrel export.
  </action>
  <verify>TypeScript compiles: bun run build</verify>
  <done>ConfigRow renders appropriate input for each data type, CollapsibleSection expands/collapses</done>
</task>

<task type="auto">
  <name>Task 3: Assemble WeaponConfigTab with all sections</name>
  <files>src/components/weapons/WeaponConfigTab.tsx, src/components/weapons/index.ts, src/app/page.tsx</files>
  <action>
Create WeaponConfigTab that:
1. Renders WeaponSelector at top
2. Shows message if no weapons selected ("Select a weapon to configure")
3. When weapons selected, renders CollapsibleSections for each group:
   - General (defaultOpen: true)
   - Strike, AltStrike, Stab, AltStab (defaultOpen: false)
4. Each section maps WEAPON_CONFIG_OPTIONS[group] to ConfigRow components
5. Value/staged changes call store actions for ALL selected weapons (multi-edit)

Wire into page.tsx: Replace placeholder in weapons TabsContent with WeaponConfigTab.

For multi-weapon editing: when value changes, loop through selectedWeapons and call setWeaponValue for each.
Display the value from the FIRST selected weapon (users understand values may differ across weapons).
  </action>
  <verify>bun run build succeeds, bun run dev starts without errors</verify>
  <done>WeaponConfigTab renders in weapons tab, shows selector and config sections</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Weapon configuration tab with multi-select and collapsible sections</what-built>
  <how-to-verify>
    1. Run: bun run dev
    2. Visit: http://localhost:3000
    3. Verify Weapons tab is selected by default
    4. Check: WeaponSelector shows GreatSword checkbox
    5. Select GreatSword - config sections should appear
    6. Expand Strike section - verify config options render with appropriate inputs
    7. Toggle a Boolean option - verify Switch works
    8. Change a Float value - verify number input works
    9. Check/uncheck a staged checkbox - verify it toggles
    10. Collapse/expand sections - verify animation works
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` succeeds without errors
- [ ] `bun run dev` starts and page loads
- [ ] Weapon selector toggles weapon selection
- [ ] Config sections show for selected weapons
- [ ] All data types render appropriate inputs
- [ ] Staged checkboxes toggle independently
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Human verified visual appearance
- Multi-weapon selection updates all selected weapons
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui/02-02-SUMMARY.md`
</output>
