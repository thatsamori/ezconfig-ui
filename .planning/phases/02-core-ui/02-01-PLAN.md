---
phase: 02-core-ui
plan: 01
type: execute
---

<objective>
Create type-specific input components for all config data types (Boolean, Float, Vector, Vector2D, FloatArray).

Purpose: These reusable components are the building blocks for both weapon and character config tabs.
Output: 5 input components in src/components/config/ that integrate with the Zustand store.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./02-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

@src/lib/config/types.ts
@src/lib/store/configStore.ts
@src/components/ui/switch.tsx
@src/components/ui/input.tsx

**Tech stack available:** Next.js 16, React 19, Tailwind CSS 4, Shadcn UI, Zustand 5
**Established patterns:** Shadcn components in src/components/ui/, store in src/lib/store/
**Data types from schema:** Boolean, Float, Vector (X/Y/Z), Vector2D (X/Y), FloatArray (dynamic length)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BooleanInput and FloatInput components</name>
  <files>src/components/config/BooleanInput.tsx, src/components/config/FloatInput.tsx, src/components/config/index.ts</files>
  <action>
Create BooleanInput wrapping Shadcn Switch - accepts value (boolean), onChange callback, disabled prop.
Create FloatInput wrapping Shadcn Input type="number" - accepts value (number), onChange callback, disabled prop, optional step/min/max.
Both components are controlled (value comes from parent, onChange reports back).
Create barrel export index.ts for all config components.
Use consistent prop interface: { value: T; onChange: (value: T) => void; disabled?: boolean }
  </action>
  <verify>TypeScript compiles without errors: bun run build</verify>
  <done>BooleanInput renders Switch, FloatInput renders number Input, both export from index.ts</done>
</task>

<task type="auto">
  <name>Task 2: Create Vector and Vector2D input components</name>
  <files>src/components/config/VectorInput.tsx, src/components/config/Vector2DInput.tsx, src/components/config/index.ts</files>
  <action>
Create VectorInput for 3D vectors - renders 3 FloatInput components in a row labeled X, Y, Z.
Value type: { x: number; y: number; z: number } or [number, number, number] (support both, normalize internally).
Create Vector2DInput for 2D vectors - renders 2 FloatInput components labeled X, Y.
Value type: { x: number; y: number } or [number, number].
Use flex layout with gap-2, labels above each input.
Update barrel export.
  </action>
  <verify>TypeScript compiles: bun run build</verify>
  <done>VectorInput shows X/Y/Z inputs, Vector2DInput shows X/Y inputs, both handle array or object format</done>
</task>

<task type="auto">
  <name>Task 3: Create FloatArrayInput component</name>
  <files>src/components/config/FloatArrayInput.tsx, src/components/config/index.ts</files>
  <action>
Create FloatArrayInput for dynamic-length float arrays.
Renders a list of FloatInput components with index labels (0, 1, 2...).
Add "+" button to append new value (default 0), "-" button on each row to remove.
Value type: number[].
Use flex-col layout, each row has input + remove button.
Minimum 1 element (disable remove when length is 1).
Update barrel export.
  </action>
  <verify>TypeScript compiles: bun run build</verify>
  <done>FloatArrayInput renders dynamic list of number inputs with add/remove functionality</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` succeeds without errors
- [ ] All 5 components export from src/components/config/index.ts
- [ ] No TypeScript errors in IDE
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Components follow consistent prop interface
- Ready for use in weapon/character config tabs
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui/02-01-SUMMARY.md`
</output>
