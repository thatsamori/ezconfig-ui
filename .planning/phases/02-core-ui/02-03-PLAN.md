---
phase: 02-core-ui
plan: 03
type: execute
---

<objective>
Build the character configuration tab with collapsible sections for Movement, Combat, and General groups.

Purpose: Enable users to edit character configuration values with staged checkboxes.
Output: Complete CharacterConfigTab component reusing patterns from weapon tab.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./02-03-SUMMARY.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-ui/02-02-SUMMARY.md

@src/lib/config/characterConfigSchema.ts
@src/lib/config/types.ts
@src/lib/store/configStore.ts
@src/components/config/index.ts
@src/components/weapons/ConfigRow.tsx
@src/components/weapons/CollapsibleSection.tsx

**Tech stack available:** Next.js 16, React 19, Tailwind CSS 4, Shadcn UI, Zustand 5
**Reusable components:** ConfigRow and CollapsibleSection from weapons/ (consider moving to shared location)
**Schema groups:** Movement, Combat, General (from CharacterConfigGroupName enum)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CharacterConfigTab with collapsible sections</name>
  <files>src/components/character/CharacterConfigTab.tsx, src/components/character/index.ts, src/app/page.tsx</files>
  <action>
Create CharacterConfigTab that:
1. Renders CollapsibleSections for each group:
   - Movement (defaultOpen: true)
   - Combat (defaultOpen: false)
   - General (defaultOpen: false)
2. Each section maps CHARACTER_CONFIG_OPTIONS[group] to ConfigRow components
3. Uses characterValues/characterStaged from store
4. Value changes call setCharacterValue, staged changes call setCharacterStaged

Import ConfigRow and CollapsibleSection from @/components/weapons (reuse existing).
Create barrel export index.ts.

Wire into page.tsx: Replace placeholder in character TabsContent with CharacterConfigTab.

Note: Character config is simpler than weapons - no multi-select, just single character.
  </action>
  <verify>bun run build succeeds, bun run dev starts without errors</verify>
  <done>CharacterConfigTab renders in character tab with all config sections</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Core UI - both weapon and character config tabs</what-built>
  <how-to-verify>
    1. Run: bun run dev
    2. Visit: http://localhost:3000
    3. Test Weapons tab:
       - Select GreatSword
       - Expand each section (General, Strike, AltStrike, Stab, AltStab)
       - Verify inputs render correctly for each data type
    4. Switch to Character tab:
       - Verify Movement section is expanded by default
       - Expand Combat and General sections
       - Verify inputs render correctly
    5. Test interactions:
       - Toggle Boolean switches
       - Enter Float values
       - Check/uncheck staged checkboxes
    6. Verify no console errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run build` succeeds without errors
- [ ] `bun run dev` starts and page loads
- [ ] Character tab shows all config sections
- [ ] All data types render appropriate inputs
- [ ] Staged checkboxes toggle independently
- [ ] Tab switching works smoothly
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Human verified both tabs work correctly
- Phase 2: Core UI complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ui/02-03-SUMMARY.md`
</output>
